# PrismWorker 前端环境变量示例
# 复制为 .env 后按需修改（vite 仅暴露 VITE_ 前缀变量）

# 开发代理目标：vite.config.ts 的 /api 代理指向的后端地址。
# 留空使用默认 http://127.0.0.1:8000（python -m app.runner 默认端口）
VITE_DEV_TARGET=http://127.0.0.1:8000

# API base：前端所有请求的统一前缀，dev 下被 vite proxy strip 后转发后端。
# 一般无需修改；若改为空字符串则直连后端（需后端开启 CORS）。
VITE_API_BASE=/api